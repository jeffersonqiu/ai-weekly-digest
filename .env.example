# =============================================================================
# Weekly AI Papers Digest - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NOTE: For Docker Compose, change POSTGRES_HOST from "localhost" to "db"
# =============================================================================

# -----------------------------------------------------------------------------
# Database (PostgreSQL)
# -----------------------------------------------------------------------------
POSTGRES_USER=digest
POSTGRES_PASSWORD=digest_secret
POSTGRES_DB=weekly_digest
POSTGRES_PORT=5432

# For local development (Postgres running on host or in separate container):
POSTGRES_HOST=localhost

# For Docker Compose (use the service name from compose.yaml):
# POSTGRES_HOST=db

# Full connection URL (optional - if set, this takes precedence)
# For local:  postgresql://digest:digest_secret@localhost:5432/weekly_digest
# For Docker: postgresql://digest:digest_secret@db:5432/weekly_digest
DATABASE_URL=postgresql://digest:digest_secret@localhost:5432/weekly_digest

# -----------------------------------------------------------------------------
# LLM Provider
# -----------------------------------------------------------------------------
# Options: "openai" or "ollama"
LLM_PROVIDER=openai

# OpenAI (recommended for speed and quality)
OPENAI_API_KEY=sk-your-api-key-here
OPENAI_MODEL=gpt-4o-mini

# Ollama (optional, for local/free inference)
# For Docker: use host.docker.internal instead of localhost
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2

# -----------------------------------------------------------------------------
# arXiv Settings
# -----------------------------------------------------------------------------
ARXIV_CATEGORIES=cs.AI,cs.LG,cs.CL,cs.CV
ARXIV_MAX_PAPERS=200
ARXIV_DAYS_LOOKBACK=7

# -----------------------------------------------------------------------------
# Email Notifications (SMTP)
# -----------------------------------------------------------------------------
# For Gmail: use an App Password (not your regular password)
# See: https://support.google.com/accounts/answer/185833
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_TO=recipient1@example.com,recipient2@example.com

# -----------------------------------------------------------------------------
# Telegram Notifications
# -----------------------------------------------------------------------------
# Create a bot via @BotFather and get your chat ID via @userinfobot
TELEGRAM_BOT_TOKEN=your-bot-token
TELEGRAM_CHAT_ID=your-chat-id
